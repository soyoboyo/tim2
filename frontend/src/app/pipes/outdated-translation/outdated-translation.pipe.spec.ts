import { OutdatedTranslationPipe } from './outdated-translation.pipe';

describe('OutdatedTranslationPipe', () => {
    it('create an instance', () => {
        const pipe = new OutdatedTranslationPipe();
        expect(pipe).toBeTruthy();
    });
});
