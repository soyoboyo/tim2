import {environment} from '../environments/environment';

export const API_URL = environment.API;
export const OAUTH_URL = environment.OAUTH_URL;
export const APP_CREDENTIAL = environment.APP_CREDENTIAL;
